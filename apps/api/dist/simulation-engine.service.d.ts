import { DatabaseService } from './database.service';
import { DecisionWorkerService } from './decision-worker.service';
import { RequestContext } from './domain';
import { TwinGraphService } from './twin-graph.service';
export declare class SimulationEngineService {
    private readonly database;
    private readonly graph;
    private readonly worker;
    constructor(database: DatabaseService, graph: TwinGraphService, worker: DecisionWorkerService);
    createSnapshot(ctx: RequestContext, input: Record<string, unknown>, idempotencyKey: string): Promise<Record<string, unknown>>;
    createScenario(ctx: RequestContext, input: Record<string, unknown>, idempotencyKey: string): Promise<Record<string, unknown>>;
    getSnapshot(ctx: RequestContext, snapshotId: string): Promise<Record<string, unknown>>;
    listBranches(ctx: RequestContext, scenarioId: string): Promise<Record<string, unknown>>;
    createBranch(ctx: RequestContext, scenarioId: string, input: Record<string, unknown>, idempotencyKey: string): Promise<Record<string, unknown>>;
    confirmBranch(ctx: RequestContext, scenarioId: string, branchId: string, input: Record<string, unknown>, idempotencyKey: string, ifMatch: string | undefined): Promise<Record<string, unknown>>;
    run(ctx: RequestContext, input: Record<string, unknown>, idempotencyKey: string): Promise<Record<string, unknown>>;
    getRun(ctx: RequestContext, simulationId: string): Promise<Record<string, unknown>>;
    compareRuns(ctx: RequestContext, input: Record<string, unknown>): Promise<Record<string, unknown>>;
    private branchDraft;
    private workerSnapshot;
    private workerScenario;
    private nodeInputs;
    private variables;
    private changes;
    private assertScenarioDriver;
    private scenarioKind;
    private snapshot;
    private branch;
    private simulationRun;
    private snapshotView;
    private branchView;
    private runView;
    private assertRead;
    private assertScenarioWrite;
    private assertSimulationRun;
    private resourceId;
    private stateHash;
    private commitCreate;
    private commit;
}
