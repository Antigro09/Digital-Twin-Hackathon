import { DemoStoreService } from './demo-store.service';
import { RequestContext } from './domain';
import { EventIntelligenceService } from './event-intelligence.service';
type JsonRecord = Record<string, unknown>;
export declare class AiGatewayService {
    private readonly store;
    private readonly events;
    constructor(store: DemoStoreService, events: EventIntelligenceService);
    status(context: RequestContext): Promise<JsonRecord>;
    activity(context: RequestContext, pageSizeInput?: string): Promise<JsonRecord>;
    runAgent(context: RequestContext, input: unknown, idempotencyKey: string): Promise<JsonRecord>;
    retrieve(context: RequestContext, input: unknown): Promise<JsonRecord>;
    importDocument(context: RequestContext, input: unknown, idempotencyKey: string): Promise<JsonRecord>;
    suggestions(context: RequestContext, pageSizeInput?: string, decisionInput?: string): Promise<JsonRecord>;
    reviewSuggestion(context: RequestContext, suggestionIdInput: string, input: unknown, idempotencyKey: string): Promise<JsonRecord>;
    recordLearningOutcome(context: RequestContext, input: unknown, idempotencyKey: string): Promise<JsonRecord>;
    private callWorker;
    private workerEndpoint;
    private timeout;
    private upstreamProblem;
    private validateStatus;
    private validateActivity;
    private validateAgentRun;
    private validateRetrieval;
    private validateDocumentImport;
    private validateSuggestionReview;
    private validateLearningOutcome;
    private assertSafeWorkerValue;
    private sanitizeAgentInput;
    private sanitizeOutcome;
    private offsetDateTime;
    private sanitizeJson;
    private base64Document;
    private requireAgentCapability;
    private requireAiRead;
    private requireEvidenceRead;
    private requireCapability;
    private contentPermissions;
    private internalPermissions;
    private contextEvidence;
    private contextEvidenceItem;
    private boundedContextContent;
    private sanitizeServerContext;
    private contextClassification;
    private contentHash;
    private sourceAcl;
    private validateSuggestions;
    private validateSuggestion;
    private strictRecord;
    private pageSize;
    private workerIdempotencyKey;
    private integer;
    private number;
    private boundedString;
    private requiredString;
    private enumValue;
    private uuid;
    private uuidArray;
    private sha256;
    private nonNegativeInteger;
    private responseRecord;
    private recordOrUndefined;
    private isSensitiveKey;
    private stableUuid;
    private assertTenant;
    private isAbort;
    private invalidWorkerResponse;
    private badRequest;
}
export {};
